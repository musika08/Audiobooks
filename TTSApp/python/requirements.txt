# TTSApp GPU sidecar (Phase 1: XTTS v2)
# Install on the GPU PC inside a venv. Torch must match your CUDA version:
#   https://pytorch.org/get-started/locally/
# Example (CUDA 12.1):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# torch is installed separately from the CUDA index by the app (do NOT list it here —
# listing it makes pip re-resolve torch and can hang for many minutes).
# coqui-tts is the maintained fork of the original (discontinued) "TTS" package and
# supports Python 3.12+. It keeps the same `from TTS.api import TTS` import.
coqui-tts
chatterbox-tts
fastapi
uvicorn
numpy
