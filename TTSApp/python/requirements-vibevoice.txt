# VibeVoice (Microsoft, experimental) — installed into its own venv (.venv-vibevoice)
# torch + torchaudio matched from the CUDA index (avoids torchaudio "WinError 127").
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.4.1
torchaudio==2.4.1
# Community fork (the official repo was taken down). 'sdpa' attention is used so flash-attn
# is NOT required (it rarely builds on Windows).
vibevoice @ git+https://github.com/vibevoice-community/VibeVoice.git
transformers
accelerate
fastapi
uvicorn
numpy
