# VibeVoice (Microsoft, experimental) — own venv (.venv-vibevoice).
# Install from the community repo ZIP so git is NOT required on the user's machine.
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.4.1
torchaudio==2.4.1
vibevoice @ https://github.com/vibevoice-community/VibeVoice/archive/refs/heads/main.zip
transformers
accelerate
fastapi
uvicorn
numpy>=1.26
