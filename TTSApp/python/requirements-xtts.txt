# XTTS v2 (Coqui) — installed into its own venv (.venv-xtts-v2)
# torch + torchaudio MUST come from the same CUDA index or torchaudio's C extension
# fails with "WinError 127". The extra-index lets pip pull both matched from cu121.
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.4.1
torchaudio==2.4.1
coqui-tts
# Pin transformers to a version that still has `isin_mps_friendly` (coqui-tts needs it).
transformers==4.46.3
fastapi
uvicorn
numpy>=1.26
# Optional de-reverb/denoise for cloned output (used only when enabled in Settings).
deepfilternet
