# XTTS v2 (Coqui) — own venv (.venv-xtts-v2). torch + torchaudio matched from cu121.
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.4.1
torchaudio==2.4.1
coqui-tts==0.27.5
# coqui-tts needs transformers>=4.57 but its XTTS code imports isin_mps_friendly, which
# transformers removed in 5.x. Cap below 5 so we land on the last 4.x (4.57.x) that has it.
transformers>=4.57,<5
librosa>=0.10,<0.11  # pitch-preserving speed change (post-synthesis time-stretch)
fastapi==0.128.0
uvicorn==0.49.0
numpy==1.26.4
