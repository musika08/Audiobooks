# Chatterbox (Resemble) — own venv (.venv-chatterbox).
# chatterbox-tts pins torch==2.6.0 (CUDA 12.4) and numpy<1.26, so we let it choose and
# just point pip at the cu124 wheel index (don't pin torch/numpy ourselves).
--extra-index-url https://download.pytorch.org/whl/cu124
chatterbox-tts==0.1.7
librosa>=0.10  # pitch-preserving speed change (post-synthesis time-stretch)
fastapi==0.128.0
uvicorn==0.49.0
