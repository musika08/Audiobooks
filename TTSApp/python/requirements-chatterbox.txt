# Chatterbox (Resemble) — installed into its own venv (.venv-chatterbox)
# torch + torchaudio matched from the CUDA index (avoids torchaudio "WinError 127").
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.4.1
torchaudio==2.4.1
chatterbox-tts
fastapi
uvicorn
numpy>=1.26
# Optional de-reverb/denoise for cloned output (used only when enabled in Settings).
deepfilternet
