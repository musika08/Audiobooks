# Fish / OpenAudio (experimental) — installed into its own venv (.venv-fish-opus)
# torch + torchaudio matched from the CUDA index (avoids torchaudio "WinError 127").
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.4.1
torchaudio==2.4.1
fish-speech
librosa>=0.10  # pitch-preserving speed change (post-synthesis time-stretch)
fastapi==0.128.0
uvicorn==0.49.0
numpy==1.26.4
